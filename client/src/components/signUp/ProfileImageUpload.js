const ProfileImageUpload = () => {
	return (
		<div>
			<img src="" alt="" />
			<button>Select image from device</button>
			<button>Confirm</button>
		</div>
	);
};

export default ProfileImageUpload;
